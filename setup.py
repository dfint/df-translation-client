"""
Stub setup.py. Used only to install df_translation_client as a package for testing.
"""

from setuptools import setup

setup(
   name='df_translation_client',
   packages=['df_translation_client']
)
